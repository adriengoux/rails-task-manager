<h1>All my tasks</h1>

<ul></ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> <%= task.completed %></li>
  <% end %>
</ul>


<p><%= link_to "new task", new_task_path %></p>
